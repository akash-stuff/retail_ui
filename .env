# Base URL of the deployed backend API (no trailing slash).
# Local development default (used automatically if this file is absent): http://localhost:5000
VITE_API_URL=https://retail-backend-g4y8.onrender.com
